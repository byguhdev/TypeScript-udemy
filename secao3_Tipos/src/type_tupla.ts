// Tupla não possui no JS e sim apenas com TS

let aluno: [string, number]; // precisa seguir a mesma ordem passada.

aluno = ["Henrique", 0]; // Só pérmite adicionar 2 elementos
aluno.push("Math", 1); // Mais eu posso adicionar mais com o push.

console.log(aluno);
