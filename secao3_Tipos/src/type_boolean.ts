// Type BOOLEAN

let auth: boolean = true;

let codeStatus: string = '1';

// tudo que for diferente de zero, string vazia, undefined, será verdadeiro.
auth = Boolean(codeStatus);

console.log(auth);
