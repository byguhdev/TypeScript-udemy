// Type Object

let newUser: object;

newUser = {
  nome: "Henrique",
  sobrenome: "Santos",
  idade: 17.8,
};

let podeSerAssim: object = {
    title: "O objeto pode ser assim..."
};

console.log(newUser);
console.log(podeSerAssim);
